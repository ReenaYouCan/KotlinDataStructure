package graph.bipertite

import hashmap.GroupOfPeople

class IsGraphBipertite {
    fun isGrphBipertite(graph: Array<IntArray>): Boolean {
        val color = Array<Int>(graph.size) { -1 }
        // Need to create graph from given array
        // Take color
        // There would be graph.size nodes
        // We need to color each and every node in graph to identify if it it can be bipertite
        return false
    }

    fun helper(currNode:Int,currNodeColor:Int,graph: Array<Int>)
    {

    }
}